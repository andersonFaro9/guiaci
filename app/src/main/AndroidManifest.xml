<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="br.com.guiacistore">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.CALL_PHONE"/>
    <uses-permission android:name="android.permission.VIBRATE"/>

    <application

        android:allowBackup="true"
        android:icon="@mipmap/guiaci"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/guiaci"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/guiaci" />
        <!-- Set color used with incoming notification messages. This is used when no color is set for the incoming
             notification message. See README(https://goo.gl/6BKBk7) for more. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/colorAccent" />

        <activity   android:screenOrientation="portrait" android:name=".splash.SplashActivity" android:theme="@style/theme_splash">

            <intent-filter>
                    <action android:name="android.intent.action.MAIN"/>
                    <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity  android:screenOrientation="portrait" android:name=".lojas.MainActivity"
            android:label="@string/app_name" android:theme="@style/AppTheme"/>

        <!--<service android:name=".MyFirebaseMessagingService">-->

            <!--<intent-filter>-->
                <!--<action android:name="com.google.firebase.INSTANCE_ID_EVENT" />-->
            <!--</intent-filter>-->

        <!--</service>-->

        <service android:name=".NotificationFirebaseMessagingService">

            <intent-filter>
                    <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>

        </service>

        <!--Usar este trecho de código para determinar se a orientação da tela deve ficar fixa em uma orientação específica

            android:screenOrientation="portrait"
        -->



        <activity  android:name=".lojas.ChocolateActivity" android:parentActivityName=".lojas.ChocolateActivity" />

        <activity    android:parentActivityName=".lojas.PadariaActivity"  android:name=".lojas.PadariaActivity" />

        <activity  android:parentActivityName=".lojas.LocadoraDeCarrosActivity" android:name=".lojas.LocadoraDeCarrosActivity" />
        <activity   android:parentActivityName=".lojas.FarmaciaActivity" android:name=".lojas.FarmaciaActivity" />

        <activity    android:name=".utils.Invisible" />

        <activity   android:name=".model.IFirebase" />

        <activity android:name=".lojas.SobreActivity" android:theme="@style/barra_sobre" />
        <activity android:name=".StorageActivity" />

        <meta-data  android:name="com.google.android.geo.API_KEY" android:value="@string/google_maps_key" />

        <meta-data android:name="preloaded_fonts"  android:resource="@array/preloaded_fonts" />

    </application>

</manifest>